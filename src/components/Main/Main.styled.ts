import styled from 'styled-components';
import { rem } from '../../styles/theme';

export const MainStyled = styled.main`
  max-width: ${rem(1024)};
  padding: ${rem(48)};
  margin-left: auto;
  margin-right: auto;
  width: 100%;
  flex: 1 0 auto;
`;
