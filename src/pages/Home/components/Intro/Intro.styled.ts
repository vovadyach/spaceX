import styled from 'styled-components';
import { rem } from '../../../../shared/styles/theme';

export const IntroStyled = styled.div`
  text-align: center;
`;

export const IntroFigureStyled = styled.figure`
  max-width: ${rem(300)};
  margin: 4rem auto;
`;
